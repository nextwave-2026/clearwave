# Status

Append-only progress log. Newest entries at the bottom. Never edit or delete an existing entry.

This file is declared `merge=union` in `.gitattributes`, so all four contributors can append freely without
conflicts. **Every entry names who wrote it** - use your GitHub username (`derek`, `andres`, `juank`, or
`raul`), because there is no shared supervisor across the four independent machines and an unattributed
line is close to useless.

**Commit this file straight to `main`** - no branch, no pull request. That is the point of it: a
progress line everyone else needs now is useless sitting in review. If your push is rejected because
they pushed first, run `git pull --no-rebase && git push`; union merge keeps both entries.

Write here whenever you finish something everyone else should know about. A decision everyone else
must build around goes in `DECISIONS.md` instead; the current shape of a boundary goes in
`INTERFACES.md`.

Format: one line per entry.

```
- <ISO 8601 UTC timestamp>  <who>  <what happened, and what it means for everyone else>
```

## Log

- 2026-08-24T01:31Z  derek  Preparation kit repository created and seeded: nextwave-2026/nextwave-kit, public, its own independent history. Declared pre-existing IP.
- 2026-08-24T01:45Z  derek  Kit complete and merged to its main. Contains the Makefile target contract, CI workflow, offline licence inventory generator, coordination files, and the three deliverable templates. No application scaffold and no language commitment, by design - the stack depends on the challenge.
- 2026-08-24T01:47Z  derek  Kit verified independently rather than on trust: `make licences` proven against both Node and Python manifests, idempotent, hand-written policy survives regeneration, no network access needed. Union merge proven with simultaneous appends from two branches - both survived, no conflict.
- 2026-08-24T01:57Z  derek  THIS entry repository created: nextwave-2026/clearwave, public. Name is deliberately product-like and domain-agnostic; do not rename it to match the challenge.
- 2026-08-24T01:59Z  derek  Kit infrastructure COPIED into this repository, never forked. Verified no common ancestor between the two histories, which is what keeps the kit pre-existing IP. Provenance declared in the first commit message and the README "Pre-existing components" section - do not remove either.
- 2026-08-24T02:00Z  derek  Continuous integration verified GREEN on the real service, not assumed: all five named checks passed on the first commit. Templates placed where they will be used - README.md and ARCHITECTURE.md at the root, docs/pitch.md.
- 2026-08-24T02:05Z  derek  Orientation written to AGENTS.md and this log started. andres: read AGENTS.md first - it carries the Makefile target contract, the licence prohibition, the history-separation rule that keeps the kit ours, and the merge behaviour of each coordination file.
- 2026-08-24T02:05Z  derek  PENDING: andres has two unaccepted GitHub invitations - owner of the nextwave-2026 organisation, and admin on this repository. Pushing from that side is blocked until both are accepted. andres: accept them and confirm here.
- 2026-08-24T02:05Z  derek  NEXT for andres: prove one end-to-end run on that side - dispatch a worker, land a green pull request, and report the round trip and anything that surprised you here. derek's side is already proven. This is the last unverified part of the whole arrangement.
- 2026-08-24T02:20Z  derek  Coordination files now commit STRAIGHT TO MAIN - no branch, no pull request. A status line the other side needs now is useless sitting in review. Safe because STATUS.md and DECISIONS.md are union-merged: verified that two sides committing to main from a stale base recover with a plain `git pull --no-rebase && git push`, both entries surviving. Code still goes through a pull request. See the Working conventions section of AGENTS.md.
- 2026-08-29T16:52Z  derek  Challenges published and Challenge 02 (Control Tower) chosen. All work targets that objective; full brief is `docs/challenge.md`.
- 2026-08-29T16:52Z  derek  Team grew to four: `juank` (GitHub `juank115`) and `raul` (GitHub `raulhiguerac`) joined `derek` and `andres`. Use those four handles for attribution.
- 2026-08-29T16:52Z  derek  Repository documentation brought in line with the chosen challenge (`docs/challenge.md`, `README.md`, `AGENTS.md`). Read those before building.
- 2026-08-29T16:52Z  derek  Architecture and stack are still undecided. Next thing that needs a human call. Do not start building components before that decision lands.
- 2026-08-29T18:09Z  derek  baseline PRD published at \; ownership boundaries and contract owners are documented in \, and PR #4 is open: https://github.com/nextwave-2026/clearwave/pull/4
- 2026-08-29T18:09Z  derek  stack decision and person-to-workstream assignment are now the two blockers to starting parallel implementation; the contracts and integration gate are ready
- 2026-08-29T18:09Z  derek  baseline PRD published at `docs/prd.md`; ownership boundaries and contract owners are documented in `docs/ownership.md`, and PR #4 is open: https://github.com/nextwave-2026/clearwave/pull/4
- 2026-08-29T18:09Z  derek  done: PR https://github.com/nextwave-2026/clearwave/pull/4
- 2026-08-29T18:11Z  derek  the STATUS.md entry timestamped 2026-08-29T18:09Z whose paths render as bare backslashes was corrupted by a tooling error and is void and superseded by the correctly rendered entry; the `done: PR` line was an internal status-protocol line written in error and is not a team status entry
- 2026-08-29T18:15Z  derek  baseline PRD and ownership boundaries merged to main, the four workstreams now have named owners, and the stack decision is the remaining blocker before parallel work opens

- done: coordination files reconciled to four contributors
- 2026-08-29T19:38Z  andres  W2 detection design settled and recorded in DECISIONS.md: detection statistic and floors, severity as a business-impact-only function, event-time bucketing behind a watermark, canonical-schema invariants, and a minimum-operational-backend build order. Nothing here changes the published C2 or C3 contracts.
- 2026-08-29T19:38Z  andres  INTERFACES.md C2 and C3 entries refreshed: they still said "not yet specified" while docs/contracts/evidence-tools.md and docs/contracts/incident.md specify both in full. They now point at those documents, which remain the single definition.
- 2026-08-29T19:38Z  andres  GAP for derek: C2 has no time-series tool. No tool returns a metric over a series of time buckets, but C3 requires an onset, severity needs a trajectory, and the demo has to answer "since when". W2 proposes adding a metric_timeseries tool to the C2 surface; say if you would rather it be folded into cohort_metrics.
- 2026-08-29T19:38Z  andres  GAP for derek: nothing yet defines what starts an investigation. The agent runtime is decided but not its trigger. W2 proposes publishing the C3 record on lifecycle transitions only (detected, and any severity escalation), with a poll of the incident store as the always-available fallback so a transport failure cannot silently stop investigations. Needs your agreement as integration owner before either of us builds it.
- 2026-08-29T19:38Z  andres  FOR RAUL: the C1 request sent earlier assumed W1 emits a normalised event and is superseded by the 19:04Z ingestion model, since normalisation is now W2's. Three asks survive and still matter: register every native shape so W2 can write the mappers, key the raw topics so all attempts of one payment stay together and in order, and provide replayable backfill history - a contextual baseline cannot be learned from the minutes before a judge fires an incident.
- 2026-08-29T19:43Z  derek  W2's transport question is answered in ADR 0004 and the Persistence and query behaviour section of `docs/l4-investigation-prd.md`; a `metric_series` tool is proposed for andres to accept or reject.
- 2026-08-29T20:00Z  derek  W3 is building the evidence gateway as the sole caller of C2 tools and assigner of every `query_id`, the evidence trail, the SQLite investigation store for results and trails, and the deterministic hypothesis pre-filter; W2 owns measurement and C2 tool implementations behind the contract and should NOT build a query gateway, citation trail, investigation store, hypothesis logic, or root-cause logic - W3 consumes W2 tools exactly as the published contract defines them.
- 2026-08-29T20:16Z  derek  Root Makefile now includes `make/*.mk`; each workstream owns its own fragment and should add its targets THERE rather than to the root Makefile; the shared contract targets stay in the root and must not be moved or renamed. This exists because three branches collided on the root Makefile today.
- 2026-08-29T20:17Z  andres  QUESTION for derek: `docs/contracts/evidence-tools.md` publishes `external_status` as C2 tool 9, and `docs/ownership.md` gives C2 to W2, but the same `docs/ownership.md` also gives W3 "external corroboration adapters (provider health, status pages, public outage signals)" and your ADR 0003 is titled "External corroboration is an evidence tool" - those two readings disagree, and W2 is about to implement the other C2 tools for real, so we do not want to build `external_status` twice or leave it an unowned stub. W2 is happy to build it if that is what you want, but you need it for the investigation harness and you own the external corroboration seam, so the offer is yours to take - please answer here or in DECISIONS.md. So nobody waits on it: W2 is implementing the other nine C2 tools now and will leave `external_status` on its fixture until you answer.
- 2026-08-29T20:17Z  andres  decision: your `metric_series` proposal (STATUS.md 19:43Z) is ACCEPTED, as an eleventh C2 tool, not folded into `cohort_metrics` - folding it in would change the response shape of a tool two other workstreams have already built against, and rule 4 in `docs/ownership.md` keeps contract changes additive during the build window. W2 will publish `metric_series` in `docs/contracts/evidence-tools.md`; the implementation is landing in a separate pull request.

- 2026-08-29T19:44Z  raul  reworking: W1 producer worker (`worker/`) is being rearchitected to match derek's C1/C1b split (DECISIONS.md 19:04Z-19:17Z) - dropping the single shared Avro schema across merchants in favor of one raw topic per merchant (`raw.<merchant_id>`), each with its own JSON Schema (no Avro, per derek's 19:04Z entry). Previously verified end-to-end against real Kafka + Schema Registry under the old single-topic Avro design; that design is superseded, not the verification method. andres: raw topics will be `raw.merchant-a`, `raw.merchant-b`, `raw.merchant-c` - subscribe by the `raw.*` pattern for your C1b normalizer rather than hardcoding names
- 2026-08-29T20:09Z  raul  FOR ANDRES: before your 19:38Z note reached me, I had already built and verified end-to-end (real Kafka + Schema Registry, jsonschema-validated) against your original C1 request in README-FOR-RAUL.md - one uniform shape per event type shared across all three merchants (`payments.attempts` with full retry chains, `payments.closed` firing only once a chain stops, `ops.telemetry`), all keyed as you asked. Full context, the specific question, and the alternative I'm not defaulting to are in `docs/adr/0014-w1-raw-events-share-one-schema-per-topic.md`. Not touching worker/ further until this is settled.
- 2026-08-29T20:22Z  derek  andres's `external_status` ownership question is answered in `DECISIONS.md` and reflected in `INTERFACES.md`: W2 should leave it on its fixture and build the other nine C2 tools; W3 implements it behind the unchanged contract.
- 2026-08-29T20:25Z  andres  ADR NUMBER CLAIM: W2's seven detection-plane records take 0015-0021 (checked `origin/main` and both open PR heads first - derek has 0013, raul's open #22 claims 0014, 0015-0021 unclaimed anywhere). PR #17 was accidentally auto-merged into PR #14's branch instead of landing on main, so the previous 0013-0019 draft never reached main and is being unwound; the detection-plane doc and these seven ADRs are being re-cut as a fresh pull request against `main` at 0015-0021.
- 2026-08-29T20:32Z  andres  W2 wired nine C2 tools to real measurement over the SQLite store - `cohort_metrics`, `cohort_compare`, `drilldown`, `decline_breakdown`, `retry_stats`, `operational_metrics`, `confounding_check`, `incident_history`, `financial_impact` - and published `metric_series` as the eleventh. The wire protocol, the error envelope and the `query_id` algorithm are byte-identical, so every `query_id` already cited still resolves. `external_status` is untouched and stays W3's.
- 2026-08-29T20:32Z  andres  FOR DEREK AND JUANK: the C2 tools now read one SQLite store located by the `CLEARWAVE_DB` environment variable, defaulting to `state/clearwave.db` relative to the working directory. Point your runner at the same file and you read the same numbers the detector reports. `python3 -m detector seed && python3 -m detector detect` fills a store with the repository's own deterministic synthetic events and writes a C3 record with `lifecycle_state: detected`.
- 2026-08-29T20:32Z  andres  FOR DEREK AND JUANK: an empty store is now a well-formed answer, not a failure - zero counters, null where a rate is undefined, an empty list, and a stated reason when an incident is not stored. Build for that shape rather than assuming a tool only answers when it has data; `scripts/ci/slice_contract.sh` drives every tool over an empty store on every CI run and stays green.
- 2026-08-29T20:35Z  andres  FOR DEREK: `metric_series` is published in `docs/contracts/evidence-tools.md` and runnable at `stubs/evidence/metric_series.py`, but `ALLOWED_TOOLS` in `investigation/gateway.py` still lists ten tools, so the gateway refuses it. That file is W3's and I have not touched it - please add `metric_series` to the allowlist. Verified in the meantime that the full `opening_bundle` for a real stored C3 record returns seven measured responses with citations that verify.
- 2026-08-29T20:43Z  derek  juank: W4 surfaces had no work started with hours remaining, so derek is building a first working version to protect the demo. It is offered to you to take over, extend or replace. It deliberately contains no domain logic, so it does not constrain your design. If you are already building, say so here and the duplicate will be dropped immediately.
- 2026-08-29T20:49Z  juank  FOR DEREK: already building W4 - please drop the duplicate. Slack notification is done and live-tested end to end: workspace, `#control-tower` channel, Incoming Webhook, and a redesigned Block Kit payload (severity-coloured header, dividers, icon fields, diagnostic confidence kept lexical and visually separate from severity per PRD section 11). The C5 payload shape and the severity-to-channel binding are drafted against `docs/contracts/incident.md` and `docs/contracts/investigation-result.md`. A dashboard view model covering all 4 views and a render script exist and run end to end against `stubs/slice.py`. None of this is pushed yet - it was built in a personal workspace outside this repository, which is exactly why it was invisible. Opening a pull request shortly; writing C5 into `INTERFACES.md` first since that is the actual gate.
- 2026-08-29T20:49Z  juank  FOR ANDRES AND DEREK: ran the real detector for the first time (`python -m detector seed && python -m detector detect`) and its `blast_radius` field names do not match `docs/contracts/incident.md` - the real output has `affected_countrys` where the contract says `affected_countries`, `affected_merchant_ids` where the contract says `affected_merchants`, and two undocumented extra fields, `affected_issuing_banks` and `affected_payment_methods`. Not fixing it myself, `detector/` is not my tree - flagging so the contract or the detector output gets reconciled before my dashboard reads the real store instead of the offline stub.
- 2026-08-29T21:07Z  juank  GAP for andres: my TAM-facing Slack alert (C5) needs a way to surface the single highest-value transaction behind an incident when one exists, so a TAM can look it up directly. `payment_id` and `amount` exist on the canonical event (`docs/contracts/canonical-event.md`) but no C2 tool, and neither C3 nor C4, ever returns an individual payment identifier - everything published today is cohort-level aggregate. Not inventing an id in the alert, that would fabricate evidence; requesting the field instead. Proposal: either add `largest_transaction` (`payment_id`, `amount`, `currency`) to `financial_impact`'s C2 output and to C3's `financial_impact` block, or a small new C2 tool that returns the top N transactions by amount for a cohort/window. Either shape works for me; say which you would rather build, or say no and I will drop the idea.
- 2026-08-29T20:49Z  derek  LICENCE QUESTION for the team: raul's W1 introduces certifi under MPL-2.0, the project's only non-permissive licence entry. MPL-2.0 is weak, file-level copyleft: it requires sharing modifications to MPL files themselves, does not reach merely dependent code, and does not force organisers to release their own code - the prohibition's target. certifi is a ubiquitous, unmodified transitive dependency for Python HTTPS. However, the written policy names MIT, Apache-2.0, BSD variants and ISC as acceptable and prohibits GPL, AGPL and other strong copyleft, but does not cover MPL either way. A human ruling is needed before submission because the licence inventory is graded. Options: accept it and state that explicitly in policy; reject it and find an HTTPS transport avoiding certifi (likely impossible); or accept silently, the weakest graded-artifact option. This is NOT currently blocking raul's work.
- 2026-08-29T20:53Z  andres  Nudge on the 20:35Z ask, still true against `origin/main`: `ALLOWED_TOOLS` in `investigation/gateway.py` still lists ten tools and refuses `metric_series`, published in `docs/contracts/evidence-tools.md` and runnable at `stubs/evidence/metric_series.py` (merged in #26). Derek proposed the tool, W2 accepted and built it, and W2's narrative needs it for incident onset and severity trajectory. It's a one-line add in W3's file, which is exactly why it's easy to leave until demo day - W2 has not touched `investigation/gateway.py` because it isn't W2's tree; please add it.
- 2026-08-29T20:53Z  andres  Observation, not urgent: `investigation/store.py` (W3) and `detector/store.py` (W2) both define `CREATE TABLE IF NOT EXISTS incident (...)`, identical column-for-column today so nothing is broken. The incident table is W2's half of C3, and rule 3 in `docs/ownership.md` wants exactly one definition in the owner's tree; because both sides use `IF NOT EXISTS`, whichever process opens the file first wins and the other silently accepts a table it didn't define, so a future column change on either side can diverge without an error at the seam - it would surface later as a data bug rather than a schema mismatch. No action needed tonight: W2 will announce any change to that table here before making it, and if derek would rather import the schema from W2's module than restate it, W2 will expose it however suits his tree.
- 2026-08-29T20:53Z  derek  juank: dropping the unpushed W4 duplicate as you asked. No pull request will be opened from this attempt. W4 stays yours.
- 2026-08-29T20:58Z  derek  juank: `origin/fm/clearwave-w4-surfaces` is a reference offer with no pull request and no claim on W4. It was written before your 20:49Z claim was visible, not in competition with it. It contains a stdlib `http.server` read-only API over the shared store, a four-view dashboard in plain HTML/CSS/JS with no framework and no build step, a severity-to-channel escalation binding, a judge-trigger adapter, and tests. It holds no domain logic: every figure is read from the incident or investigation record, never computed. Take, adapt or ignore any part of it. Anything you already have working takes precedence, including your live-tested Slack integration. You claimed the layer correctly; this is a hand-up, not a second attempt.
- 2026-08-29T21:20Z  juank  Pull request opened as promised at 20:49Z, this time going through review instead of straight to `main`: `juank/w4-surfaces` (https://github.com/nextwave-2026/clearwave/compare/main...juank/w4-surfaces), built on derek's `fm/clearwave-w4-surfaces` hand-up rather than duplicating it - took the dashboard, escalation binding, store and judge-trigger adapter as-is, and filled in the two channels derek left as placeholders. Slack now posts a real Block Kit message (severity and diagnostic confidence in separate blocks, per PRD section 11) instead of the raw payload; phone now places a real Twilio Programmable Voice call over `urllib` (no SDK) with a silent `<Pause>` TwiML when `CLEARWAVE_TWILIO_*` credentials are set, falling back to the dashboard pending-call panel otherwise - the "no habla" call design from my personal notes, kept because a Twilio trial account narrates its own "trial account" notice before any TwiML runs regardless. `docs/contracts/notification-escalation.md` drafts C5 in full against the actual code, following the C2/C3/C4 documentation pattern; `INTERFACES.md`'s C5 entry stays untouched until this merges, so it never points at an unmerged doc. `stubs/slice.py` stage 5 now calls the real `surfaces.escalation`/`surfaces.present` instead of printing fixed lines - verified locally, all five stage markers still green. Also fixed a Windows-only test file-lock in `tests/test_surfaces.py` (an unclosed sqlite connection blocked tempdir cleanup); no behaviour change. Review welcome from anyone - derek, andres, raul.
- 2026-08-29T21:21Z  derek  FOR JUANK: canonical demo orientation is `docs/demo-sequence.md`. Your W4 approach is the accepted path, including the live-tested Slack Block Kit integration and the Twilio Programmable Voice call. Warning: the detector currently emits `blast_radius.affected_merchant_ids` and `affected_countrys` while `docs/contracts/incident.md` says `affected_merchants` and `affected_countries`; the dashboard should read the contract names, and that mismatch is andres to fix. Your request for a high-value transaction id on C2/C3 (`STATUS.md` 2026-08-29T21:07Z) is acknowledged and belongs to andres.
- 2026-08-29T21:27Z  andres  FOR RAUL: your 20:09Z question is answered in `DECISIONS.md` - the uniform one-shape-per-topic contract stands, rebuild nothing. Separately: your frozen `provider_timeout` code was being silently dead-lettered by W2's validation instead of normalised, so W2 is fixing it as a mapper translation (your enum is untouched); and your backfill-history increment is the one W2 concretely needs next, ahead of the live judge trigger. Full reasoning in `DECISIONS.md`; `docs/adr/0014-...md` is yours to mark Accepted once derek confirms he's releasing his 19:04Z wording.
- 2026-08-29T21:45Z  andres  THE PIPELINE IS CONNECTED. W2 now consumes W1's three live Kafka topics into the same SQLite store everything else already reads, so the deterministic measurement, detection, localisation, pricing and severity run on real traffic instead of a file. `detector/consumer.py`, one new CLI command, in pull request `fm/clearwave-w2-kafka-consumer`. How to run it, end to end from a clean checkout: `docker compose up -d kafka schema-registry`, then `pip install -r detector/requirements.txt`, then `python3 -m worker.worker merchant-a --mode anomaly --interval-seconds 0.2` in one shell, then `python3 -m detector consume --seconds 60 --detect` in another - that last command is live traffic to a stored C3 record with money attached, in one step. `make live` is the same thing. Full operator detail in `docs/live-ingestion.md`.
- 2026-08-29T21:45Z  andres  HONEST LIMIT on the above, read this before you rely on it: this environment has no Docker, no podman and no JVM, so I could NOT bring the stack up and I have NOT seen a message travel from a running worker to the detector. What I did verify: the `confluent_kafka` consumer constructs, accepts its configuration, subscribes to the three topics and polls and closes cleanly against an unreachable broker; the Schema Registry wire framing our decoder reads is pinned by a test against the client library's own constants; and the whole consumer loop - routing, deduplication, dead-lettering, offset-commit ordering, and detection through to a priced C3 record - is driven by 29 offline tests over the exact byte shapes W1 publishes. What is unproven is only the last hop: real broker, real Schema Registry, real worker. Whoever has Docker tomorrow morning, please run the four commands above and say here whether it worked. If it does not, `python3 -m detector seed && python3 -m detector detect` is unchanged and imports no Kafka client at all, so the demo has a fallback that cannot be broken by a broker.
- 2026-08-29T21:45Z  andres  FOR RAUL: your `provider_timeout` is fixed on W2's side and your enum is untouched, as promised at 21:27Z. It maps to C1b's `timeout` and the native spelling is preserved in `provider_raw_code`. I did the whole enum rather than just that one value, and `provider_timeout` was the only mismatch - the other seventeen already lined up. The real deliverable is a test that reads `worker/registry/payment_attempt.schema.json` directly and fails if the two vocabularies ever drift again, in either direction. So: add a decline reason freely, and `make test` will tell you immediately that W2 has to map it. Before this, the symptom was silence - the attempts were dead-lettered and simply vanished from the counts, which is exactly what would have happened to the retry storm in the guaranteed provider-degradation demo.
- 2026-08-29T21:45Z  andres  FOR RAUL, unchanged ask: replayable backfill history is still the one thing W2 concretely needs from W1. Without it the baseline stays the crude trailing window, and a seasonal baseline cannot be learned from the minutes before a judge fires an incident. Also FYI, non-blocking: your `ops.telemetry` samples now have a consumer - they are the only first-party source of service-level runtime health, and `operational_metrics.runtime_health` reports them instead of always answering `unobserved`.
- 2026-08-29T21:45Z  andres  FOR DEREK AND JUANK: nothing you have built changes shape. `operational_metrics.runtime_health` gains real values when telemetry has been consumed and still answers `unobserved` with its reason when it has not, so the empty-store behaviour you built against is byte-identical. `service_health` is untouched and stays derived from attempts - if the two ever disagree, that is a finding, not a bug. `payments.closed` is consumed and stored in a `payment_closed` table but no C2 tool reads it yet and none of your numbers move; the reasoning is in `DECISIONS.md`. juank: your 21:07Z high-value-transaction ask is acknowledged and not in this pull request - say whether you want it on `financial_impact` or as its own tool and I will build whichever you name.
- 2026-08-29T21:45Z  andres  TRAP worth knowing before someone loses twenty minutes to it: `make licences` reads the licence metadata installed in whatever `python3` runs it, so regenerating the inventory on a machine with packages installed in the system interpreter adds transitive rows CI's clean interpreter will not produce, and `scripts/ci/licences_current.sh` then fails on a file that looked correct locally. Regenerate with `python3 -S scripts/licences.py` and the output matches CI. Noted in `AGENTS.md`; I have not changed the Makefile target, because forcing `-S` there would silently kill transitive-dependency coverage and that inventory is graded.
- 2026-08-29T21:42Z  derek  the licence question raised at 20:49Z is settled as accepted; `AGENTS.md` now states the policy explicitly, and the licence inventory needs no further change
- 2026-08-29T21:42Z  derek  FOR RAUL AND ANDRES: the 19:04Z per-merchant-shape wording is released, ADR 0014 is now Accepted, and raul is unblocked; derek moved the record to Accepted directly because the code freeze is close and nobody should wait on a status field
- 2026-08-29T21:35Z  derek  starting investigation model configuration upgrade: add runtime model discovery, safe reasoning-effort support, configurable output ceiling, and regression coverage; bounds remain unchanged
- 2026-08-29T22:18Z  derek  ADR 0022 records why L4 uses the bounded OpenAI Responses API loop with strict Pydantic C4 output instead of a general-purpose coding-agent harness, including the real advantages a harness would have for open-ended exploration and remediation
- 2026-08-29T22:36Z  derek  FOR RAUL: the guaranteed scenarios have no runnable injection path. Verified against current code: `detector/cli.py:67` sets SCENARIOS = ("healthy", "provider_incident", "confounded"); `python3 -m detector --db /tmp/qa-verify-confounded.db seed --scenario confounded` accepted 1600 events; subsequent `detect` returned `incident: null` (the `tests/synthetic.py:107` confounded() uses constant ~0.9 conversion with provider/bank correlation but no time-based degradation). No high-impact generator in cli or synthetic. `docs/scenarios.md:8` lists the three guaranteed scenarios, so the documented set and the runnable set disagree. raul: make W1's hidden injector able to generate the three committed scenarios, keeping the hidden truth (C6) private and never passing a scenario identifier downstream to the investigation layer. Note that derek is separately patching the two detector localisation defects (entry 3) so those are not raul's to fix. The judge trigger itself is NOT part of this ask - raul is already building it and that work is not in question here.
- 2026-08-29T22:37Z  derek  FOR ANDRES: the blast-radius emitter still needs the real fix. juank reported the mismatch at 2026-08-29T20:49Z; it was ruled "fix both sides". derek's half has landed (PR https://github.com/nextwave-2026/clearwave/pull/33, merged): investigation reads both spellings defensively. The emitter half is still open. `detector/metrics.py:570` builds these keys programmatically as `result[f"affected_{dimension}s"]` (inside blast_radius), which produces `affected_countrys` and `affected_merchant_ids` where `docs/contracts/incident.md:35-36` specifies `affected_countries` and `affected_merchants`. andres: correct the emitter side so the contract names are what the detector actually publishes. derek's shim is deliberately tolerant of both spellings so andres's fix cannot break the investigation layer whenever it lands.
- 2026-08-29T22:38Z  derek  FOR ANDRES, informational: derek is patching two detector localisation defects. `detector/` is andres's tree; this is offered rather than imposed. It will arrive as a reviewable pull request rather than a push to main, and andres should say so here if he would rather own it - the branch will be dropped immediately, exactly as the W4 duplicate was on 2026-08-29T20:53Z. The code freeze at 2026-08-30 15:30Z is the reason derek is not waiting. The QA pass proved the defects by running the real detector over generated replays: (1) the published high-impact scenario is dropped before C3 - Merchant A's own absolute drop (0.02530) and z-score (-4.36) pass every check, but localisation does not descend because Merchant A's separation from its healthy sibling measured 0.0536 against `LOCALISE_MIN_SEPARATION=0.10` (`detector/config.py:73`; `detector/detect.py:217`); result: no incident, no investigation, no dashboard entry, no ranking evidence. (2) the confounded scenario's C3 cohort stops at `{provider: provider-p2}` and omits `issuing_bank`, so `evaluator.score_diagnosis` returns recall 0.5 and verdict `fail` against the cohort `docs/scenarios.md` requires.
- 2026-08-29T22:39Z  derek  FOR RAUL (correction to 22:36Z entry): Entry 1 was written before PR 34 (https://github.com/nextwave-2026/clearwave/pull/34, merged in f06a2c5) was visible; raul has already built the injection path and the three scenario definitions (worker/inject.py + worker/ground_truth/scenarios.py + worker/ground_truth/store.py + docker-compose services). The scenario-generation ask is covered. Residue, each verified against the PR34 branch + current main:
1. FOR JUANK AND RAUL: the judge trigger is still not connected end to end. `worker/inject.py` docstring says it is 'callable directly for now until W4 wires a real trigger to it'; `surfaces/inject.py` still returns {"wired": false, "fired": false, "message": "injection is not wired"}. W1's half now exists; someone has to join the two.
2. FOR RAUL: the C6 ground-truth SQLite lives inside the worker container (DEFAULT_DB = .../worker/ground_truth/state/ground_truth.db at `worker/ground_truth/store.py:22`) with no volume mount in `docker-compose.yml` for any worker-* service, so the evaluator (outside the container) has no read path at all. The quarantine is correct in spirit but currently total: nothing can score a run. Ask how he wants the evaluator to reach it.
3. FOR RAUL, informational, low priority: PR 34 replaces the invented merchant and bank names with real companies (merchant-a="Liverpool" in worker/profiles/merchant_a.py; banks include "BBVA Mexico", "Nu Brasil", "Itau" in worker/reference/banks.py). Note it neutrally as something the team may want to reconsider before a public submission, since the demo depicts those named companies having payment failures on fabricated data. Do not argue a position; just put it on the record for a human to decide.

Entries 2 (22:37Z) and 3 (22:38Z) are unaffected.
- 2026-08-29T22:47Z  derek  FOR JUANK: the end-to-end judge trigger has two halves and no owner for the seam between them, so it has stayed unbuilt while each side waited. That seam is now assigned to juank. W1's half exists and works: `worker/inject.py` publishes an incident-control command to a running worker, and its own docstring says it is "callable directly for now until W4 wires a real trigger to it". W4's half does not: `surfaces/inject.py` still returns `wired: false`, and the dashboard reports that failure to the judge. The seam is therefore W4's to close, which matches `docs/ownership.md`: the control surface is W4's and the injection endpoint behind it is W1's, and W4 calls it rather than reimplementing injection. juank: please say here if you see a concrete reason this should sit elsewhere; the default is yours so it does not stall again. raul remains available for the W1 side of the seam. This assignment is about who closes the gap, not a criticism of anyone.
- 2026-08-29T22:48Z  derek  FOR ANDRES: the 22:47Z assignment of the judge-trigger seam to juank is superseded. That seam is assigned to andres. He is working with juank on the dashboard, which puts him on both sides of the gap between `worker/inject.py` and `surfaces/inject.py`. andres: please say here if you see a concrete reason this should sit elsewhere; the default is yours so it does not stall again. juank remains the W4 collaborator on this work; raul remains available for the W1 side. This assignment is about who closes the gap, not a criticism of anyone.
- 2026-08-29T22:52Z  derek  FOR RAUL: the hidden-truth evaluator access problem is yours. Verified against current main: `worker/ground_truth/store.py` writes its SQLite file to `worker/ground_truth/state/ground_truth.db` inside the worker package; `docker-compose.yml` defines no volume for any `worker-*` service, so during a live containerised run that file exists only inside each worker container and is destroyed with it; each of the three merchant workers has its own separate, isolated copy; `evaluator/score.py` takes the hidden-truth record as an argument (`score_diagnosis(diagnosis, hidden_truth)`) and has no reader of its own, so something must fetch the record and hand it over. Offline, everything shares one filesystem and scoring works. During a live containerised demo the evaluator has no read path at all, so a live run cannot be scored. Proposed direction, for you to evaluate rather than follow blindly: expose the hidden truth to the evaluator over a dedicated internal Docker network that only the worker and the evaluator join, keeping it off any network the detector or investigation services can reach. A Docker network carries a service, not a file, so this implies a small read-only endpoint in front of the ground-truth store rather than sharing the SQLite file directly. That is arguably a stronger isolation guarantee than a host volume mount, because a mounted file is readable by anything running on the host, whereas network membership is an explicit, auditable allow-list, and a read-only endpoint can also refuse to answer before a diagnosis exists. A host volume mount remains the simpler fallback if the network approach does not fit the time remaining before the freeze; please say here which you are taking. Whichever route you pick, the isolation guarantee must survive: detection and investigation must still have no read path to the hidden truth. That is the property the team is claiming in `INTERFACES.md` and `docs/contracts/hidden-truth.md`.
- 2026-08-29T23:46Z  derek  FOR JUANK AND ANDRES: the captain has ruled that andres's TAM mockup (`surfaces/static/tam-dashboard.html`) is the design source of truth for the dashboard. juank's live dashboard (`surfaces/static/index.html`) remains the working product and the real data path; this is a design adoption onto it, not a replacement of his layer and not a rejection of his work. derek is doing the port now and it will arrive as a reviewable pull request, because the freeze is close. juank owns W4 and should say here if he wants to own or redirect this; the branch will be dropped immediately, exactly as the W4 duplicate was on 2026-08-29T20:53Z. andres's mockup file stays in the tree as the design reference.
- 2026-08-30T00:23Z  derek  FOR ANDRES: your judge-trigger seam on PR 45 is exactly what the demo needed and is being landed, not questioned. The live dashboard was rebuilt from your TAM mockup on the captain's ruling (PR 44) while your branch was open, so a merge as-is would conflict or silently revert that design. derek is rebasing `fm/clearwave-w2-judge-trigger-and-backfill` onto current main: the rebuilt dashboard wins on design, your trigger behaviour (toggle state, live target label, state fetch on load, fire/stop) is preserved and re-applied onto the new markup. Say here if any of that reconciliation is wrong.
