# Exact pinned resolution (pip freeze from a clean venv), not >= ranges -
# so the build is reproducible and the licence inventory stays true to what
# actually resolves. Includes transitive dependencies as direct entries
# because scripts/licences.py reads manifest lines, not the dependency tree.
anyio==4.14.2
attrs==26.1.0
Authlib==1.7.2
cachetools==7.1.7
certifi==2026.7.22
cffi==2.1.1
confluent-kafka==2.15.0
cryptography==50.0.1
Faker==40.37.0
h11==0.16.0
httpcore==1.0.9
httpx==0.28.1
idna==3.19
joserfc==1.7.5
jsonschema==4.26.0
jsonschema-specifications==2025.9.1
pycparser==3.0
referencing==0.37.0
rpds-py==2026.6.3
typing_extensions==4.16.0
